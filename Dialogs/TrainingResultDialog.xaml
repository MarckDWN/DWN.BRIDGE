<Window x:Class="AIBridge.Dialogs.TrainingResultDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:Tx="http://unclassified.software/source/txtranslation"
        Title="{Tx:T Skill Generation Result}" Height="450" Width="650"
        WindowStartupLocation="CenterOwner"
        Background="#1E1E2E"
        Foreground="White"
        FontFamily="Segoe UI">
    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="8"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="14"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="{Tx:T Verify the new rules to apply and confirm}"
                   Foreground="#A0A0C0" FontSize="12" TextWrapping="Wrap"/>

        <TextBox Grid.Row="2" x:Name="ResultTextBox"
                 Background="#2D2D4E" Foreground="#7FEBA1" FontFamily="Consolas"
                 BorderBrush="#7B68EE" BorderThickness="1"
                 Padding="6" FontSize="13" CaretBrush="White"
                 SelectionBrush="#4A90E2" AcceptsReturn="True" TextWrapping="Wrap"
                 VerticalScrollBarVisibility="Auto"/>

        <StackPanel Grid.Row="4" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button x:Name="OkButton" Content="{Tx:T ✔ Save Skill}"
                    Width="120" Height="28" Margin="0,0,8,0"
                    Background="#27AE60" Foreground="White" BorderThickness="0"
                    Click="OkButton_Click" IsDefault="True">
                <Button.Template>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" CornerRadius="5" Padding="6,3">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Button.Template>
            </Button>
            <Button x:Name="CancelButton" Content="{Tx:T Discard}"
                    Width="80" Height="28"
                    Background="#E74C3C" Foreground="White" BorderThickness="0"
                    Click="CancelButton_Click" IsCancel="True">
                <Button.Template>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" CornerRadius="5" Padding="6,3">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Button.Template>
            </Button>
        </StackPanel>
    </Grid>
</Window>
