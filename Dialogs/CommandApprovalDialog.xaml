<Window x:Class="AIBridge.Dialogs.CommandApprovalDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:Tx="http://unclassified.software/source/txtranslation"
        Title="{Tx:T Action Required: Approve Command Execution}" MinHeight="220" Width="550"
        SizeToContent="Height"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        Background="#1E1E2E"
        Foreground="White"
        FontFamily="Segoe UI">
    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="12"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="20"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="{Tx:T The AI Agent wants to execute the following command:}" Foreground="#A0A0C0" FontSize="14" TextWrapping="Wrap"/>

        <Border Grid.Row="2" Background="#181825" BorderBrush="#E74C3C" BorderThickness="1" CornerRadius="4" Padding="10">
            <StackPanel>
                <TextBlock x:Name="WorkspaceTextBlock" Foreground="#A0A0C0" FontSize="11" TextWrapping="Wrap" Margin="0,0,0,4"/>
                <TextBlock x:Name="CommandTextBlock" Foreground="#E74C3C" FontSize="13" FontFamily="Consolas" TextWrapping="Wrap" FontWeight="Bold"/>
            </StackPanel>
        </Border>

        <StackPanel Grid.Row="4" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button x:Name="DenyButton" Content="{Tx:T ❌ Deny}"
                    Width="110" Height="32" Margin="0,0,10,0"
                    Background="#3D3D5E" Foreground="White" BorderThickness="0"
                    Click="DenyButton_Click" IsCancel="True">
                <Button.Template>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" CornerRadius="5">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Button.Template>
            </Button>
            <Button x:Name="OnceButton" Content="{Tx:T ✔ Execute Once}"
                    Width="140" Height="32" Margin="0,0,10,0"
                    Background="#F39C12" Foreground="White" BorderThickness="0"
                    Click="OnceButton_Click" IsDefault="True">
                <Button.Template>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" CornerRadius="5">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Button.Template>
            </Button>
            <Button x:Name="AlwaysButton" Content="{Tx:T 🚀 Execute Always}"
                    Width="150" Height="32"
                    Background="#27AE60" Foreground="White" BorderThickness="0"
                    Click="AlwaysButton_Click">
                <Button.Template>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" CornerRadius="5">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Button.Template>
            </Button>
        </StackPanel>
    </Grid>
</Window>
