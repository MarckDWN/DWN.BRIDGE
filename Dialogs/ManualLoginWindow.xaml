<Window x:Class="AIBridge.Dialogs.ManualLoginWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:wv2="clr-namespace:Microsoft.Web.WebView2.Wpf;assembly=Microsoft.Web.WebView2.Wpf"
        Title="Google Secure Login" Height="700" Width="900"
        WindowStartupLocation="CenterOwner"
        Background="#1E1E1E">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        
        <Border Grid.Row="0" Background="#2D2D30" Padding="15,10">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <TextBlock Grid.Column="0" Text="Effettua l'accesso a Google. La finestra si chiuderà automaticamente a login completato." 
                           Foreground="#4A90E2" FontWeight="SemiBold" FontSize="14" TextWrapping="Wrap" VerticalAlignment="Center"/>
                <Button Grid.Column="1" Content="Ho Finito (Chiudi)" Padding="10,5" Background="#4A90E2" Foreground="White" FontWeight="Bold" BorderThickness="0" Cursor="Hand" Click="CloseButton_Click"/>
            </Grid>
        </Border>

        <wv2:WebView2 x:Name="LoginWebView" Grid.Row="1" />
    </Grid>
</Window>
