<Window x:Class="AIBridge.OAuthLoginDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:tx="clr-namespace:Unclassified.TxLib;assembly=Unclassified.TxLib"
        xmlns:wv2="clr-namespace:Microsoft.Web.WebView2.Wpf;assembly=Microsoft.Web.WebView2.Wpf"
        Title="{tx:T Login with Provider}" Height="600" Width="450"
        WindowStartupLocation="CenterOwner" ResizeMode="NoResize"
        Background="#1E1E1E" Foreground="White" WindowStyle="ToolWindow">
    <Border Background="#1E1E1E" BorderBrush="#007ACC" BorderThickness="0">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="*" />
            </Grid.RowDefinitions>
            
            <Border Grid.Row="0" Background="#2D2D30" CornerRadius="10,10,0,0">
                <Grid Margin="15,10">
                    <TextBlock Text="{tx:T 🔒 Connect OAuth Account}" Foreground="White" FontWeight="SemiBold" FontSize="16" />
                    <Button Content="✕" HorizontalAlignment="Right" Background="Transparent" Foreground="#888" BorderThickness="0" Cursor="Hand" Click="CloseButton_Click" FontSize="16" Margin="0,-5,-5,0"/>
                </Grid>
            </Border>

            <wv2:WebView2 Name="OAuthWebView" Grid.Row="1" />
            
            <StackPanel Name="LoadingPanel" Grid.Row="1" HorizontalAlignment="Center" VerticalAlignment="Center">
                <TextBlock Text="⏳" FontSize="40" HorizontalAlignment="Center" Margin="0,0,0,10" />
                <TextBlock Text="{tx:T Loading...}" Foreground="#AAA" FontSize="14" />
            </StackPanel>
        </Grid>
    </Border>
</Window>
